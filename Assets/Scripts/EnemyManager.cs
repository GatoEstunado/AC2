using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyManager : MonoBehaviour
{
    /*
    public List<Enemy> lstEnemies;

    public void SetAllFlee()
    {
        foreach (Enemy item in lstEnemies)
        {
            if (item.state != EnumEnemyState.ARRIVE)
            {
                item.changeState(EnumEnemyState.FLEE);
            }
        }

        Invoke("SetAllWander", 4);
    }

    public void SetAllWander()
    {
        foreach (Enemy item in lstEnemies)
        {
            if (item.state == EnumEnemyState.FLEE)
            {
                item.changeState(EnumEnemyState.WANDER);
            }
        }
    }
    */
}
